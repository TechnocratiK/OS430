/**
 * Keypad H file for retreiving input
 *
 * Expects:
 * 1. TimerA to be running at 32768Hz
 * 2. Port 1 and 3 attached to the keypad and P1 interrupt not in use
 * 3. TimerA counting up and interrupt A0 is not in use
 * 4. TimerA will be run continuously
 */

#ifndef __INCLUDE_KEYPAD_H
#define __INCLUDE_KEYPAD_H

void keypad_init(void);
char keypad_get_char(void);

#endif
